/**
 * The exact wording shown next to the consent checkbox, stored verbatim on the
 * lead. These addresses are contacted months after capture, so we need to be
 * able to show what the person actually agreed to at the time — bump the
 * version rather than editing in place if the wording changes.
 */
export const CONSENT_VERSION = "2026-08-17";

export const CONSENT_TEXT =
    "I agree to Bhutan Upward Travels emailing me my discount code and occasional travel offers. I can unsubscribe at any time.";
